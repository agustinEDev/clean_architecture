# Dependencias HTTP para Orders Microservice

# Framework web principal
fastapi==0.104.1

# Servidor web ASGI para ejecutar FastAPI
uvicorn[standard]==0.24.0

# Validación de datos (incluido con FastAPI pero lo especificamos)
pydantic==2.5.0

# Para testing de endpoints HTTP
httpx==0.25.2

# Dependencias para PostgreSQL
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1